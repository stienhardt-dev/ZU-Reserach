## Cursor Cloud specific instructions

This repository (`ZU-Reserach`) is currently empty — it contains only a `README.md`. There are no source code files, dependencies, build systems, or services to run.

When code is added to this repository, this section should be updated with:
- Language/framework details and required system dependencies
- How to install dependencies, build, lint, and test
- How to run the application(s) in development mode
- Any non-obvious caveats discovered during setup
